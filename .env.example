# Supabase — copy to .env.local and fill in from your project (Settings → API).
# Until these are set, the app runs fully local-only (no auth, sync, or community).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
