
import React from "react";
import Layout from "@/components/Layout";
import PrayerTracker from "@/components/PrayerTracker";

const PrayerPage = () => {
  return (
    <Layout title="Prayer Tracker">
      <PrayerTracker />
    </Layout>
  );
};

export default PrayerPage;
